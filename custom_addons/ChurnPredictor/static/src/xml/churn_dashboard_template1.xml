<!-- <?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">
    <!-- Template cho Dashboard chính -->
    <t t-name="ChurnPredictor.mainDashboardTemplate">
        <div class="o_churn_dashboard p-4 bg-white">
            <h2>Churn Dashboard (Odoo 17)</h2>
            <p>Hiển thị Odoo Action và Client Action con bên trong một Client Action chính.</p>
            <div class="container-fluid mt-4">
                <div class="row">
                    <!-- Cột 1: Hiển thị Churn Dashboard Action của bạn -->
                    <div class="col-md-5 p-3 bg-light border rounded">
                        <!-- THAY ĐỔI Ở ĐÂY: Cập nhật tiêu đề -->
                        <h4>Churn Dashboard (Graph, Pivot...)</h4>
                        <div class="h-100">
                            <!-- THAY ĐỔI Ở ĐÂY: Cập nhật state trỏ tới churnAction -->
                            <ActionContainer t-if="state.churnAction" action="state.churnAction" className="'h-100'"/>
                        </div>
                    </div>

                    <!-- Cột 3: Client Action con - Giữ nguyên -->
                    <div class="col-md-3 p-3 bg-light border rounded">
                        <h4>Client Action Con</h4>
                        <SubClientAction/>
                    </div>
                </div>
            </div>
        </div>
    </t>

    <!-- Template cho Client Action con -->
    <t t-name="ChurnPredictor.subClientTemplate">
        <div class="p-3">
            <p>Đây là nội dung từ một Client Action con.</p>
            <button class="btn btn-primary" t-on-click="() => alert('Xin chào từ Sub Client Action!')">Bấm vào đây</button>
        </div>
    </t>
</templates> -->