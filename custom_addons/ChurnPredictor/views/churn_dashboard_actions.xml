<!-- <?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Client Action chính (Dashboard) -->
    <!-- Thẻ 'tag' phải khớp với tên đã đăng ký trong registry của JS -->
    <record id="action_churn_dashboard_main_client" model="ir.actions.client">
        <field name="name">Churn Unified Dashboard</field>
        <field name="tag">churn_dashboard_main_client</field>
    </record>

    <!-- Client Action con -->
    <!-- Thẻ 'tag' này chỉ dùng để JS gọi, không phải là một action độc lập hoàn chỉnh -->
    <record id="action_churn_sub_client" model="ir.actions.client">
        <field name="name">Sub Client Action</field>
        <field name="tag">churn_sub_client_action_tag</field>
    </record>

    <!-- Window Action để hiển thị một view Odoo tiêu chuẩn -->
    <!-- Chúng ta sẽ không gọi action này trực tiếp qua menu, mà sẽ nhúng nó vào dashboard -->
    <!-- Ví dụ: Lấy action danh sách khách hàng có sẵn -->
    <!-- Bạn có thể tạo một action mới nếu muốn -->
    <!-- <record id="action_for_dashboard_partners" model="ir.actions.act_window">
        <field name="name">Đối tác (cho Dashboard)</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{'search_default_customer': 1}</field>
    </record> -->

    <!-- Menuitem để mở Dashboard chính -->
    <menuitem id="menu_churn_dashboard_unified"
              name="Dashboard (Unified)"
              parent="menu_churn_root"
              action="action_churn_dashboard_main_client"
              sequence="5"/>
</odoo> -->